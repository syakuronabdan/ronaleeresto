div(style={ 'background-color': '#ccc', 'font-family': 'serif', 'padding': '20px' })
  table(align='center', border=0, cellpadding=0, cellspacing=0, width=600, style={ 'background-color': '#fff' })
    tbody
      tr
        td(style={ 'background-color': '#eee', 'border-bottom': '4px #ddd solid', 'height': '100px' })
          img(alt='app', src=config.url('/static/img/logo-email.png'))
      tr
        td(style={ 'padding': '20px' })
          h4 Hello, #{user.name}
          br
          p(style={ 'line-height': '1.2em' })
            | We've received your request to reset your password #[br]
            | Please follow the link bellow to reset your password, or simply ignore this email if this is a mistake
            | #[a(href=config.url(`/reset?token=${token.token}&user=${user.id}`), style={ 'font-size': '16pt' }) Reset My Password]
            | #[br]
            | P.S. The activation link will expire in 24 hours

          p Thanks You
